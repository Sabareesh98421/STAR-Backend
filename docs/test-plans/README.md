# Test plans

One file per major feature, named after the feature (e.g. `auth-signup.md`).
Written before the feature's `.spec.ts` files exist. See [CLAUDE.md](../../CLAUDE.md)
for how plans map to colocated spec files and when edits here trigger regeneration.
